<%= form_with(model: user_book) do |form| %>
  <% if user_book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_book.errors.count, "error") %> prohibited this user_book from being saved:</h2>

      <ul>
        <% user_book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= form.label :user_id %>
  <%= form.collection_select :user_id, User.all, :id, :username, prompt: "Select a User" %>
  </div>

  <div class="field">
  <%= form.label :book_id %>
  <%= form.collection_select :book_id, Book.all, :id, :title, prompt: "Select a Book" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
